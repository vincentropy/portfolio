"use strict";
exports.__esModule = true;
var fs = require("fs");
var mdPath = './public/content/';
console.log('reading md files from: ' + mdPath);
var files = fs.readdirSync(mdPath).filter(function (value) { return value.endsWith('.md'); });
console.log('found: ' + files.join(', '));
var outputData = {};
outputData['pages'] = files.map(function (value) { return ({ filename: value }); });
var outString = JSON.stringify(outputData);
fs.writeFileSync('./public/content_index.json', outString);
